# Roadmap

## Done
- [x] Browser-side (WebCodecs) rendering restored as the only render path
- [x] Serialized disk writes — fixes "Cannot close a ERRORED writable stream" on long renders
- [x] Per-panel colour grading + event-driven encoder back-pressure (fast 2h renders)
- [x] Parallel prompt/image pipeline across the Paralon + Pixazo key pools
